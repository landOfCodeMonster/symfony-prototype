{% extends 'base.html.twig' %}

{% block title %}Pharmacy{% endblock %}

{% block body %}
    <h1>Pharmacy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pharmacy.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pharmacy.name }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ pharmacy.location }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ pharmacy.phone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pharmacy_index') }}">back to list</a>

    <a href="{{ path('pharmacy_edit', {'id': pharmacy.id}) }}">edit</a>

    {{ include('pharmacy/_delete_form.html.twig') }}
{% endblock %}
