{% extends 'base.html.twig' %}

{% block title %}Edit Pharmacy{% endblock %}

{% block body %}
    <h1>Edit Pharmacy</h1>

    {{ include('pharmacy/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('pharmacy_index') }}">back to list</a>

    {{ include('pharmacy/_delete_form.html.twig') }}
{% endblock %}
